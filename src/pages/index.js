import React from 'react';

const Home = () => {
    return (
        <section>
            <div class="container">
                <h1>Calcula tu seguro(?)</h1>
            </div>
        </section>

    );
};

export default Home;